#include "main.h"

/**
  *main - test that the function positive_or_negative()i
  * gives the correct output when given a case of 0.
  *
  *Return: 0(success)
  */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);
	return (0);
}
