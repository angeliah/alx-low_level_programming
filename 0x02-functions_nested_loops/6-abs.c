#include "main.h"

/**
  *abs - function that computes the absolute value of an integer
  *@ab: integer input
  *
  * Return: absolute value of ab
  */
int _abs(int)
{
	return (ab * ((ab > 0) - (ab < 0)));
}
